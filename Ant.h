#pragma once
#include "Common.h"
#include<QList>
class CAnt
{
public:
    CAnt();
    CAnt(int id);
    ~CAnt(void);

public:
    int m_id;
    int m_nPath[N_CITY_COUNT]; //???????¡¤??
    double m_dbPathLength; //?????????¡¤??????

    int m_nAllowedCity[N_CITY_COUNT]; //?????????
    int m_nCurCityNo; //?????????§Ò???
    int m_nMovedCityCount; //???????????????
public:

    int ChooseNextCity(); //????????????

    void Init(); //?????
    void Move(); //????????§Þ????
    void Search(); //????¡¤??
    void CalPathLength(); //?????????????¡¤??????
    double WalkLength() const
    {
        return m_dbPathLength;
    }
    QList<int> Path() const;

};

inline bool operator ==(const CAnt& a, const CAnt& b)
{
    if(a.m_id != b.m_id) return false;
    if(a.m_nCurCityNo != b.m_nCurCityNo) return false;
    if(a.m_nMovedCityCount != b.m_nMovedCityCount) return false;
    return true;
}
